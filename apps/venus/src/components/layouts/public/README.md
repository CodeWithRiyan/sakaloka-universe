# Public Layout Quarantine

This folder belongs to the archived marketing/landing experience, not to the
active Venus application shell.

Rules:

- Do not wire these layouts back into the Venus router.
- Do not reuse this navbar/layout for `/login`, `/register`, or dashboard.
- Treat these files as layout references for the future centralized landing
  site only.

If the marketing site needs them again, migrate them to the Strapi-managed
landing workspace instead of reconnecting them to Venus.
