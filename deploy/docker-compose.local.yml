# Sakaloka-Universe — Local testing (build from source)
# Usage:
#   cd deploy
#   docker compose -f docker-compose.dev.yml -f docker-compose.local.yml up -d

services:

  sakaloka-api:
    image: sakaloka-api:local
    build:
      context: ..
      dockerfile: apps/api/Dockerfile